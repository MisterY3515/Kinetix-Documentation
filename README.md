# Kinetix Documentation

Interactive web documentation for the Kinetix programming language.

## Usage

Open `index.html` in a browser.

## Structure

- `css/` — Stylesheets
- `js/` — Core application logic
- `data/` — Documentation modules (one file per section)
